{% extends "base.html.twig" %}

{% block body %}
    <div class="container">
        {{ form_start(form, {'multipart': true}) }}
            <input type="file" name="portrait">
            <input type="file" name="full_body">
            <button class="btn btn-secondary btn-block">Envoyer ma candidature</button>
        {{ form_end(form) }}
    </div>
{% endblock %}