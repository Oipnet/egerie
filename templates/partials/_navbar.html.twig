<nav class="navbar fixed-top navbar-light bg-black navbar-expand-md navigation">
    <a href="{{ path('homepage') }}" class="navbar-brand">
        <img class="mx-auto d-blockalign-middle" src="{{ asset('build/images/casting-logo.png') }}" alt="Casting sun7">
    </a>
    <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav mx-auto">
            <li class="nav-item">
                <a href="{{ path('homepage') }}"
                   class="nav-link
                        {% if app.request.attributes.get('_route') == 'homepage' %}
                        active
                        {% endif %}"
                >
                    Accueil
                </a>
            </li>
            <li class="nav-item">
                <a href="{{ path('first_edition') }}"
                   class="nav-link
                        {% if app.request.attributes.get('_route') == 'first_edition' %}
                        active
                        {% endif %}"
                >
                    Première édition
                </a>
            </li>
            <li class="nav-item">
                <a href="{{ path('homepage') }}"
                   class="nav-link
                        {% if app.request.attributes.get('_route') == 'casting' %}
                        active
                        {% endif %}"
                >Casting 2018</a>
            </li>
            <li class="nav-item">
                <a href="{{ path('register') }}"
                   class="nav-link
                        {% if app.request.attributes.get('_route') == 'register' %}
                        active
                        {% endif %}"
                >
                    Inscription
                </a>
            </li>
            <li class="nav-item">
                <a href="{{ path('contact') }}"
                   class="nav-link
                        {% if app.request.attributes.get('_route') == 'contact' %}
                        active
                        {% endif %}"
                >
                    Contact
                </a>
            </li>
        </ul>
    </div>
</nav>